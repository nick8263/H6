<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UserApp.Pages.QuestionPage"
             Title="QuestionPage">
    <ScrollView>
        <VerticalStackLayout
    
    Padding="30,0"
    Spacing="25">

            <VerticalStackLayout x:Name="questionStack">

            </VerticalStackLayout>

            
            <Label x:Name="ValidationLabel" TextColor="Red" IsVisible="False" />
            <Button
                 x:Name="SubmitBtn"
                 Text="Submit" 
                 Clicked="OnSubmitClicked"
                 HorizontalOptions="Fill" />
            <Button                
                Text="Cancel" 
                Clicked="CancelClicked"
                HorizontalOptions="Fill" />
            
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>